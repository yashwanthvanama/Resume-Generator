# Resume Generator Dependencies
# Python packages required for the resume generator project

# PDF text extraction for reading original resume PDFs
PyPDF2==3.0.1

# File watching for automatic recompilation of templates
watchdog==2.1.6

# Additional packages that might be useful for future enhancements
# (uncomment if needed)
# pathlib2==2.3.7  # Enhanced pathlib functionality (if needed for older Python versions)